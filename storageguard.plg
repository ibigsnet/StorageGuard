<?xml version='1.0' standalone='yes'?>
<!DOCTYPE PLUGIN [
<!ENTITY name "StorageGuard">
<!ENTITY author "RifleJock">
<!ENTITY version "2026.09.08aa">
<!ENTITY launch "Settings/StorageGuard">
<!ENTITY pluginURL "https://raw.githubusercontent.com/ibigsnet/StorageGuard/testing/storageguard.plg">
<!ENTITY github "ibigsnet/StorageGuard">
<!ENTITY raw "https://raw.githubusercontent.com/ibigsnet/StorageGuard/testing">
<!ENTITY plugdir "/usr/local/emhttp/plugins/StorageGuard">
<!ENTITY configdir "/boot/config/plugins/StorageGuard">
]>

<PLUGIN name="&name;"
  author="&author;"
  version="&version;"
  launch="&launch;"
  pluginURL="&pluginURL;"
  icon="shield"
  min="6.12.0"
  support="https://forums.unraid.net/topic/199796-plugin-storage-guard-free-space-thresholds-so-you-know-if-a-failed-disk-still-leaves-room-to-move-data/"
  project="https://github.com/&github;"
  readme="https://raw.githubusercontent.com/&github;/testing/README.md">

<CHANGES>
###&version;
- Alerts check is POST+csrf (Main poll no longer starts notifies on GET).

###2026.09.06aa–ab
- Hashed raw install (SHA256/MD5); Settings threshold note escapes form values.

###2026.08.31aa
- RAID0/single/DUP/one-disk pools paint Critical (cannot survive a disk loss). Alerts once per level change, not hourly.

###2026.08.17ah
- Settings: Array → Alerts → Cache order; post-install autofill disks; Yes defaults; green outline OK, pulse off.

###2026.08.17ag–ae
- Cache thresholds Show Cache; empty pool = None (until ah autofill); pulse default No.

###2026.08.17ad–ab
- Pools-only Settings HTML; product defaults Outline/green OK; capacity labels = Main.

### Older releases
- Full changelog: https://github.com/ibigsnet/StorageGuard/blob/main/CHANGELOG.md
</CHANGES>

<FILE Run="/bin/bash">
<INLINE><![CDATA[
echo "StorageGuard: preparing..."
# Note: do not delete /tmp/plugins/storageguard.plg here — Unraid install working copy
mkdir -p /boot/config/plugins/StorageGuard
# YYYY.MM.DDxx package names: always remove prior installs so upgrades never leave mixed files
for p in /var/log/packages/StorageGuard-*; do
  [ -e "$p" ] || continue
  echo "StorageGuard: removing old package $(basename "$p")"
  removepkg "$(basename "$p")" 2>/dev/null || true
done
rm -rf /usr/local/emhttp/plugins/StorageGuard \
       /usr/local/emhttp/plugins/storageguard 2>/dev/null || true
rm -f /boot/config/plugins/StorageGuard/StorageGuard-*.txz 2>/dev/null || true
mkdir -p /usr/local/emhttp/plugins/StorageGuard
echo "StorageGuard: ready."
]]></INLINE>
</FILE>

<!-- raw-files begin -->
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/StorageGuard.page">
<URL>&raw;/StorageGuard.page?v=&version;</URL>
<SHA256>3ed7b25a72335db889ef7874a28bfc46b83faaaba910ebba008729e44f668294</SHA256>
<MD5>981f91befc8d5a3777bd0bb975de65d5</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/default.cfg">
<URL>&raw;/default.cfg?v=&version;</URL>
<SHA256>d4d7286afd69b2aaa924a6e1476b39427e57624bf7d8519db918d97844f18b18</SHA256>
<MD5>db2f8540518cb5b4767577b3ce0c4e1a</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/README.md">
<URL>&raw;/README.md?v=&version;</URL>
<SHA256>cbf2ccab683cd6f07669668ca14ea2cce9758ade366432fb2f8ff45ae35a5d4d</SHA256>
<MD5>3630739e71bf42ed8e12536955e706f1</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/sg-update.php">
<URL>&raw;/sg-update.php?v=&version;</URL>
<SHA256>b84ae5efc8fa1b5fc5daa29bf879a222b383dbf54537e9fe0696e219e4663eea</SHA256>
<MD5>83c55bb0612e1b66e15dc959b8543182</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/storageguard.js">
<URL>&raw;/storageguard.js?v=&version;</URL>
<SHA256>33edddf2e17818b37ed63d2235678fc382af9eceeccc2650b729122c1e89e442</SHA256>
<MD5>83fa36b4f3cece4a28a0b41f0161d0ea</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/storageguard.css">
<URL>&raw;/storageguard.css?v=&version;</URL>
<SHA256>47829df059e2bd84a59be5b209bee72a8c26759bb6e87f69d466c1ab979d7a3e</SHA256>
<MD5>01f4c08dadf25c2f6719bf1cd35b1db9</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/storageguard-color.js">
<URL>&raw;/storageguard-color.js?v=&version;</URL>
<SHA256>bd12beeaab139564f65e2fd9f0150cfc033f8a9e7e533388e7ad27bbc0ebad04</SHA256>
<MD5>99eade83e9f91a966da5776b368f0c77</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/get-config.php">
<URL>&raw;/get-config.php?v=&version;</URL>
<SHA256>1d89953e6ac30e23f3aeb67e5c8a1d3d0080af982baa728a5d6e380fe12ccae7</SHA256>
<MD5>4c426d2190432b0141136f8e57236856</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/check-alerts.php">
<URL>&raw;/check-alerts.php?v=&version;</URL>
<SHA256>5b66342a6fd8a611376f05063c6b75cc81e70baf5f0f063178d1e1790816a6d4</SHA256>
<MD5>53976567a97586233fed6ca3ea8934fa</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/sg-lib.php">
<URL>&raw;/sg-lib.php?v=&version;</URL>
<SHA256>683b9ba5f644248887cb82df9905a4dc9c78eb023cd9e7ae352abe40eb1b1bd0</SHA256>
<MD5>5ee5634a813fc4889a0ed33d39e4d237</MD5>
</FILE>
<FILE Name="/usr/local/emhttp/plugins/StorageGuard/sg-pool-math.php">
<URL>&raw;/sg-pool-math.php?v=&version;</URL>
<SHA256>2431adb99e10564c0b25940ee005a003c884582b0299eb8ef54cccd6572b83f3</SHA256>
<MD5>e023014d0826b835d4f9b7db11adf6de</MD5>
</FILE>
<!-- raw-files end -->

<FILE Run="/bin/bash">
<INLINE><![CDATA[
echo "StorageGuard: finishing install..."
if [ ! -f /usr/local/emhttp/plugins/StorageGuard/sg-lib.php ]; then
  echo "StorageGuard: ERROR — package did not install (missing sg-lib.php)"
  exit 1
fi
VER=""
for candidate in /tmp/plugins/storageguard.plg /boot/config/plugins/storageguard.plg; do
  if [ -f "$candidate" ]; then
    VER=$(sed -n 's/.*ENTITY version "\([^"]*\)".*/\1/p' "$candidate" | head -1)
    [ -n "$VER" ] && break
  fi
done
[ -z "$VER" ] && VER="unknown"
mkdir -p /boot/config/plugins/StorageGuard
# Machine-aware product defaults (autofill disks, Yes toggles, green outline / no pulse)
php -r '
require_once "/usr/local/emhttp/plugins/StorageGuard/sg-lib.php";
$path = "/boot/config/plugins/StorageGuard/StorageGuard.cfg";
$fresh = !is_file($path);
if ($fresh && is_file("/usr/local/emhttp/plugins/StorageGuard/default.cfg")) {
  @copy("/usr/local/emhttp/plugins/StorageGuard/default.cfg", $path);
}
if (function_exists("sg_seed_product_cfg")) {
  // Fresh install: full seed. Upgrade: fill missing/empty threshold keys only.
  sg_seed_product_cfg($fresh);
  echo $fresh ? "StorageGuard: seeded product defaults on flash\n" : "StorageGuard: healed empty defaults on flash\n";
}
' 2>/dev/null || {
  if [ ! -f /boot/config/plugins/StorageGuard/StorageGuard.cfg ] \
     && [ -f /usr/local/emhttp/plugins/StorageGuard/default.cfg ]; then
    cp -a /usr/local/emhttp/plugins/StorageGuard/default.cfg /boot/config/plugins/StorageGuard/StorageGuard.cfg
  fi
}
# Main free-bar CSS/JS: ONLY these Unraid layout files (absolute paths).
# Never walks /mnt, shares, or any folder named "Storage Guard".
# Marker-only strip (no broad "Storage Guard" text match).
SG_MARKER='StorageGuard-inject'
SG_BACKUP=/boot/config/plugins/StorageGuard/stock-backup
mkdir -p "$SG_BACKUP"
sg_strip_inject() {
  local f="$1"
  [ -f "$f" ] || return 0
  # Drop our marker block + legacy inject lines (plugin asset paths only)
  sed -i \
    -e "/${SG_MARKER}/d" \
    -e '/storageguard-color\.js/d' \
    -e '/storageguard\.css/d' \
    -e '/plugins\/StorageGuard\/storageguard/d' \
    "$f" 2>/dev/null || true
}
for f in \
  /usr/local/emhttp/webGui/include/DefaultPageLayout/HeadInlineJS.php \
  /usr/local/emhttp/plugins/dynamix/include/DefaultPageLayout/HeadInlineJS.php
do
  [ -f "$f" ] || continue
  base=$(basename "$f")
  if [ ! -f "$SG_BACKUP/$base.stock" ]; then
    cp -a "$f" "$SG_BACKUP/$base.stock"
    echo "StorageGuard: backed up stock $base"
  fi
  sg_strip_inject "$f"
done
HI="/usr/local/emhttp/webGui/include/DefaultPageLayout/HeadInlineJS.php"
if [ -f "$HI" ]; then
  {
    printf '\n<!-- %s begin -->\n' "$SG_MARKER"
    printf '<link rel="stylesheet" href="/plugins/StorageGuard/storageguard.css?v=%s">\n' "$VER"
    printf '<script src="/plugins/StorageGuard/storageguard-color.js?v=%s"></script>\n' "$VER"
    printf '<!-- %s end -->\n' "$SG_MARKER"
  } >> "$HI"
  echo "StorageGuard: injected into HeadInlineJS (marker ${SG_MARKER})"
fi
echo "StorageGuard: packages now: $(ls /var/log/packages/StorageGuard-* 2>/dev/null | xargs -n1 basename 2>/dev/null | tr '\n' ' ')"
echo "StorageGuard: install complete. Hard-refresh the browser (Ctrl+Shift+R)."
]]></INLINE>
</FILE>

<FILE Run="/bin/bash" Method="remove">
<INLINE><![CDATA[
echo "StorageGuard: removing..."
# Strip inject from fixed layout paths only — never array/share paths
SG_MARKER='StorageGuard-inject'
for f in \
  /usr/local/emhttp/webGui/include/DefaultPageLayout/HeadInlineJS.php \
  /usr/local/emhttp/plugins/dynamix/include/DefaultPageLayout/HeadInlineJS.php \
  /usr/local/emhttp/webGui/include/HeadInclude.php \
  /usr/local/emhttp/plugins/dynamix/include/HeadInclude.php
do
  [ -f "$f" ] || continue
  sed -i \
    -e "/${SG_MARKER}/d" \
    -e '/storageguard-color\.js/d' \
    -e '/storageguard\.css/d' \
    -e '/plugins\/StorageGuard\/storageguard/d' \
    "$f" 2>/dev/null || true
done
for p in /var/log/packages/StorageGuard-*; do
  [ -e "$p" ] || continue
  removepkg "$(basename "$p")" 2>/dev/null || true
done
rm -rf /usr/local/emhttp/plugins/StorageGuard
# Full uninstall: flash config + stock backups + runtime scratch (clean reinstall)
rm -rf /boot/config/plugins/StorageGuard
rm -rf /tmp/storageguard_alerts
rm -f /tmp/plugins/storageguard.txt 2>/dev/null || true
# Note: do not delete /tmp/plugins/storageguard.plg here — Unraid install working copy
# Note: do not touch /boot/config/plugins-removed/ (Unraid-owned)
# Note: does not touch user shares, /mnt, or folders named "Storage Guard"
echo "StorageGuard: removed."
]]></INLINE>
</FILE>

</PLUGIN>
