Menu="StorageGuard:90"
Title="Help"
Tag="question-circle"
Markdown="false"
---
<?php
require_once '/usr/local/emhttp/plugins/StorageGuard/sg-page-boot.php';
sg_page_styles();
?>
<div class="sg-wrap">
  <p class="sg-section-lead">
    Thresholds are remaining <strong>free space</strong>. Main free-bar paint and Unraid notifications are independent.
    <strong>None</strong> turns that level off.
  </p>
  <ul style="line-height:1.5;margin:0.4em 0 0.8em 1.25em;padding:0">
    <li><strong>Array</strong> (hidden if no array disks) and each pool tab (named like <strong>Cache</strong>) set Warning/Critical, alerts, coloring, and outline vs fill for that target.</li>
    <li><strong>Settings</strong> — outline vs fill for <em>all</em> targets, green outline when OK, pulse on warn/crit.</li>
    <li><strong>Suggest free thresholds</strong> on a pool tab fills Custom from capacity-fit Δ when that Δ is real (RAID10 / RAID5/6 / 3+ disk RAID1). Hidden on 2-disk RAID1 — survivor holds a copy; Custom still works.</li>
    <li>Pools that cannot survive a disk loss (RAID0, single, DUP, one disk) stay Critical on Main.</li>
    <li><strong>Default</strong> resets the whole plugin. Apply on a tab saves that tab only.</li>
  </ul>
  <p>
    <a href="https://github.com/ibigsnet/StorageGuard/blob/main/DOCS.md" target="_blank" rel="noopener">DOCS.md</a>
    · <a href="https://github.com/ibigsnet/StorageGuard/blob/main/docs/math/threshold-guide.md" target="_blank" rel="noopener">threshold guide</a>
    · <a href="https://forums.unraid.net/topic/199796-plugin-storage-guard-free-space-thresholds-so-you-know-if-a-failed-disk-still-leaves-room-to-move-data/" target="_blank" rel="noopener">Unraid forum</a>
  </p>
</div>
